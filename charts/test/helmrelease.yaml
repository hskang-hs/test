apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: my-nginx-test
  namespace: nginx
spec:
  chart:
    spec:
      chart: ./charts/nginx
      sourceRef:
        kind: GitRepository
        name: hs-test
        namespace: ci-cd-test-5vgdm
      interval: 1m
  interval: 5m